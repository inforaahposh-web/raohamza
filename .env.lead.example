# Email notify when a lead is submitted (optional but recommended)
# Get a key at https://resend.com — add in Vercel → Environment Variables
RESEND_API_KEY=
LEAD_FROM_EMAIL=onboarding@resend.dev

# Optional override if Site info email is empty
LEAD_NOTIFY_EMAIL=
